# SQL-workflows
Statistics Denmark has effectively limited the RAM usage on its research servers - but they still deliver data in flat files. This repo is a playground for the most common steps in the data-analysis pipeline using SQL with duckdb.
